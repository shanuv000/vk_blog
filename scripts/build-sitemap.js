// This script is no longer used for generating the sitemap-news.xml file
// We now use a dynamic route at pages/sitemap-news.xml.js instead
console.log(
  "The sitemap-news.xml is now generated dynamically via pages/sitemap-news.xml.js"
);
console.log(
  "This script is kept for reference but is no longer used in the build process."
);
